services:
  - type: web
    name: predictor-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --threads 8 --worker-class gthread --timeout 120
    healthCheckPath: /api/health
    envVars:
      - key: ADMIN_USERNAME
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: HMAC_SECRET
        sync: false
      - key: STATE_FILE
        value: gambling_state.db
      - key: ALLOWED_ORIGINS
        sync: false
